// Primeiro exemplo de código em Java Script

const idade = 29; // Declaro uma constante chamada idade
const nome = "Daniel"; // Declara uma constante chamada nome
const inf = nome + " " + idade

// Imprime uma mensagem no terminal
console.log("Ola Daniel!");

// Exibindo o valor das constantes no terminal
console.log(`${nome} idade ${idade}`);
console.log(inf);