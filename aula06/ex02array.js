// Exemplo 2 com array
notas = [5,10,7.5];
console.log(notas); // Exibe as notas

// Remove a última nota do array
notas.pop();
console.log(notas);
notas.push(9); // Adciona a nota no array
console.log(notas);
notas.push(10); // Adiciona notas no array
console.log(notas);